/**
 * Returns a boolean input to disabled attribute of a button. Used to enable or disable buttons
 * @param {Boolean} value
 * @returns {Boolean}
 */
function enableDisable(value) {
  return Boolean(value) ? false : true;
}

export default enableDisable;
